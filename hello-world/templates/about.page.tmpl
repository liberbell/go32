{{template "base" .}}

{{define "content"}}
    <div class="container">
        <div class="row>
            <div class="col">
                <h1>This is the About page</h1>
                <p>This is a paragraph of text </p>
                <p>This is a paragraph of text </p>

                <p>This came from the template: {{index .StringMap "test"}}</p>
                <p>
                    {{if ne (index .StringMap "remoteIP") ""}}
                        <h4>Your remote IP address is {{index .StringMap "remote_ip}}</h4>
                    {{else}}

                    {{end}}
                </p>
            </div>
        </div>
    </div>
{{end}}